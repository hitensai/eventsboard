      <nav class="navbar navbar-default navbar-inverse">
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Project name</a>
          </div>
          <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
              <li class="<%= "active" if current_page?("/")%>"><%= link_to "Home", root_path %></li>
              <li class="<%= "active" if current_page?("/events")%>"><%= link_to "Events", events_path %></li>
              <% admins_only do %>
              <li class="<%= "active" if current_page?("/admin") %>"><%= link_to "Catogories", admin_categories_path %></li>
              <li class="<%= "active" if current_page?("/admin") %>"><%= link_to "Admins", admin_root_path%></li>
              <% end %>
               <% unless user_signed_in? %>
                  <li class="<%= "active" if current_page?("/users/sign_up")%>"><%= link_to "Sign_up", new_user_registration_path %></li>
                  <li class="<%= "active" if current_page?("/users/sign_in")%>"><%= link_to "sign_in", new_user_session_path %></li>
            
               <% end %>
      
            </ul>
            <ul class="nav navbar-nav navbar-right">
              <%  if user_signed_in?%>
                 <li class="<%= "active" if current_page?("/users/edit")%>"><%= link_to edit_user_registration_path do  %>
                   <span> Image </span> <%= current_user.username %>


                   <% end %>
                 </li>
                 <li class="<%= "active" if current_page?("/users/#{current_user.username}")%>"><%= link_to "My Events", user_path(current_user) %></li>
                  <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
              <% end %>    
            </ul>  

              
          </div><!--/.nav-collapse -->
        </div><!--/.container-fluid -->
      </nav>

      <!-- Main component for a primary marketing message or call to action -->
     
